# Copy to .env in the repo root (Docker Compose loads it for variable substitution).
# TomTom Traffic API (https://developer.tomtom.com/)
TOMTOM_API_KEY=your_key_here

# Optional pacing (api-feed; locations are fixed in src/api_feed/app.py)
# SAMPLES_PER_CYCLE=120
# CYCLE_SECONDS=600
